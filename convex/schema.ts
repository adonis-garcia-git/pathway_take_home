import { defineSchema } from "convex/server";

// Phase 0: empty schema. Tables land in Phase 1 (runs, stages, dishes, ingredients,
// prices, distributors, emailThreads, quotes, recommendations). See CLAUDE.md.
export default defineSchema({});
